#!/usr/bin/env php
<?php
//Reccupérons le port fournit lors de l'appel server
$port = isset($argv[1]) ? $argv[1] : 80;

//Créeons un exécutable qui va nous permettre de lancer notre server de développement
exec("php -S localhost:$port -t public");



